kind: Service
apiVersion: v1
metadata:
  name: {{ .Values.name }}
spec:
  selector:
    k8s-app: fluent-bit-logging
  ports:
  - name: fluent-bit-http
    protocol: TCP
    port: 2020
    targetPort: 2020
